const Ball = () => (
  <div
    data-testid="ball"
    style={{
      backgroundColor: "red",
      width: "50%",
      paddingBottom: "50%",
      position: "absolute",
      borderRadius: "100%",
      bottom: 0,
      zIndex: 0,
    }}
  />
);

export default Ball;
